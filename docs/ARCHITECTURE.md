# Architecture Mirror

The canonical architecture spec currently lives in
[`architecture.md`](./architecture.md). This file exists to preserve the
uppercase repository structure described in `copilot-instructions.md`.
